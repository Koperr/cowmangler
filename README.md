### In active development
Quake-like FPS using raylib

Submodules:

```git submodule update --init --recursive```

Build:

```sh setupCMake.sh``` 

Compile:

```sh compile.sh```

Run the program:

```sh run.sh```
